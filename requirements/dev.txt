-r base.txt

# Dev server helpers
django-debug-toolbar>=4.4,<5.0

# Tests
pytest>=8.2,<9.0
pytest-django>=4.8,<5.0
pytest-cov>=5.0,<6.0
factory-boy>=3.3,<4.0
# N+1 : fixture pytest-django `django_assert_num_queries` (équivalent demandé)

# Quality
ruff>=0.5,<1.0
mypy>=1.11,<2.0
django-stubs[compatible-mypy]>=5.0,<6.0
types-redis>=4.6,<5.0
pre-commit>=3.8,<4.0
